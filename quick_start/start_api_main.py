# parser for port and pushing to DockerHub

# 1. adjust APP_PORT and TRUSTED_CLIENT_PORT in backend.config
# 2. dockerfiles/Dockerfile.server.main  EXPOSE
# 3. dockerfile for frontend
# 4. docker compose (?)

# Build Docker image

# Start Docker container