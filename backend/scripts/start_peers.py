# parser for number of peers


# loop for each peer
    # 1. adjust PEER_PORT and TRUSTED_CLIENT_PORT backend.config
    # 2. dockerfiles/Dockerfile.server.peer EXPOSE
    # 3. dockerfile for frontend
    # 4. docker compose (?)
    # 5. Build Docker image
    # 6. Start Docker container